import pandas as pd

def transform_variables(df, **kwargs):
	'''
	Input: Pandas DataFrame, Dictionary
	Output:  Pandas DataFrame

	Transform the pandas dataframe columns using the passed in dictionary, where the keys are the column names
	and the values are options of how to transform that data (i.e. dummy it, or something like that). 
	'''
	pass
